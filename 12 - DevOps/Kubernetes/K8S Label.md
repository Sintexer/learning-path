A label is the mechanism of loosely coupling [[Kubernetes]] resources. Each time a pod is created, it is assigned a label from `spec.template.metadata.labels`. The controllers seek the pdos they will manage by querying the pods having a specific label from `spec.selector.matchLabels`. Kubernetes will actually fail with an error if a controler couldn't spot any pods by expected labels.