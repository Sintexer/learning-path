
## Eventual consistency

**Eventual consistency** is a data consistency model in distributed computing where updates to a piece of data propagate asynchronously across replicas, guaranteeing that all read requests will eventually return the last updated value if no new updates are made.

## East-West Traffic

East-west traffic is data flow between servers, virtual machines, or containers inside a single data center or private cloud network