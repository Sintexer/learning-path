A **critical section** is a part of a program that accesses a shared resource (like a variable, file, or database) that must not be accessed by more than one process or thread at the same time.

In short: **It is the "exclusive zone" where only one worker is allowed at a time to prevent errors.**